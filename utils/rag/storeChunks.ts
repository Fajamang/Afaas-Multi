export async function storeChunks(tenant: string, chunks: string[], embeddings: number[][]): Promise<void> {
  console.log(Storing \ chunks for tenant "\");
  // Hier zou je de chunks en embeddings kunnen opslaan in bijv. Supabase, een vector DB, etc.
}
